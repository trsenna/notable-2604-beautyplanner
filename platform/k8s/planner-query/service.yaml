apiVersion: v1
kind: Service
metadata:
  name: planner-query
  labels:
    app: planner-query
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: planner-query
