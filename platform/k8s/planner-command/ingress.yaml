apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: planner-command-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /$2
spec:
  tls:
  - hosts:
    - planner.localhost
    secretName: localhost-tls-secret
  rules:
  - host: planner.localhost
    http:
      paths:
      - path: /command(/|$)(.*)
        pathType: ImplementationSpecific
        backend:
          service:
            name: planner-command
            port:
              number: 8080
