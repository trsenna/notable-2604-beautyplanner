apiVersion: v1
kind: Service
metadata:
  name: planner-sync
  labels:
    app: planner-sync
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: planner-sync
